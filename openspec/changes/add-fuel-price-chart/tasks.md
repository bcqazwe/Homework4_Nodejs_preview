## 1. 基礎設施與準備

- [x] 1.1 在 `public/index.html` 的 `<head>` 引入 Chart.js CDN 腳本。
- [x] 1.2 在 `public/index.html` 中新增「趨勢分析」的 Tab 按鈕與對應的內容容器（包含 `<canvas>` 標籤）。

## 2. 圖表邏輯與數據處理

- [x] 2.1 實作圖表初始化函式，定義各油種折線的配色與基礎設定（如 `spanGaps`）。
- [x] 2.2 撰寫資料轉換邏輯：將後端回傳的 DESC 陣列轉為 ASC，並提取各油種與日期的 Dataset。
- [x] 2.3 實作極端值偵測邏輯：計算各年度各油種的最高與最低價。

## 3. 視覺化優化與互動

- [x] 3.1 利用 Chart.js 回呼函數實作最高點（紅）與最低點（綠）的高亮大小與顏色區別。
- [x] 3.2 整合「歷史查詢」與「趨勢分析」：點擊查詢按鈕時，同步更新表格與折線圖。

## 4. 驗收與測試

- [x] 4.1 驗證當年度資料不足或有缺失時，圖表是否能正確顯示（無斷裂）。
- [x] 4.2 驗證滑鼠 Hover 到高亮點時，Tooltip 顯示的數值是否正確對應。
- [x] 4.3 確保 Tab 切換過程流暢且圖表大小能自適應容器。
