## 1. 後端 API 開發

- [x] 1.1 在 `app.js` 中新增 `GET /api/all` 路由，檢索資料庫所有數據
- [x] 1.2 確保 `/api/all` 返回的資料已按日期 (`date`) 升序排列
- [x] 1.3 移除舊有的 `/api` 年份過濾限制測試 (手動 API 請求測試)

## 2. 前端介面更新

- [x] 2.1 修改 `public/index.html` 中的「趨勢分析」標籤點擊事件，使其調用 `/api/all`
- [x] 2.2 更新 `updateChart` 函式，支援自動 X 軸縮放
- [x] 2.3 調整圖表樣式：隱藏非極值點的資料點圓圈 (`pointRadius: 0`)
- [x] 2.4 實施全局 High/Low 偵測邏輯，確保標註整段歷史的極大值與極小值

## 3. 測試與驗證

- [x] 3.1 驗證跨年度數據載入是否正常
- [x] 3.2 驗證圖表上是否顯示了正確的「歷史最高」與「歷史最低」標記
